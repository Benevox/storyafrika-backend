# Dev-only: install this on Windows + Python 3.13 to avoid building psycopg2.
# Use SQLite (leave DB_NAME unset in .env). For PostgreSQL use Python 3.12 + requirements.txt.
# Django Backend Requirements for StoryAfrika (without PostgreSQL driver)
# Core Framework
Django==5.0.1
djangorestframework==3.14.0
django-cors-headers==4.3.1

# Database: psycopg2-binary omitted (no wheel for Python 3.13; use SQLite when DB_NAME is unset)

# Authentication
djangorestframework-simplejwt==5.5.1
django-allauth==0.61.1
google-auth==2.27.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0

# API Documentation
drf-spectacular==0.29.0

# Image handling
Pillow>=10.2.0,<11

# Environment variables
python-decouple==3.8

# Content & Text Processing
markdown==3.5.2
bleach==6.1.0

# AWS S3 (for file storage)
boto3==1.34.34
django-storages==1.14.2

# Development
django-debug-toolbar==4.3.0
